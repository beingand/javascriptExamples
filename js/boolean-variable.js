// Create variables and assign their values
let stock = true;
let shipping = false;

// Get the element that has an id of stock
let elStock = document.getElementById('stock');
// Set class name with value of stock variable
elStock.className = stock;

// Get the element that has an id of shipping
let elShip = document.getElementById('shipping');
// Set class name with value of shipping variable
elShip.className = shipping;